import 'package:flutter/material.dart';

@immutable
class SectionsTitle extends StatelessWidget {
  final String title;
  const SectionsTitle({required this.title, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
